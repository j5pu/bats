#!/usr/bin/env shts
#shellcheck shell=bats

@test "$(shts::basename) " { shts::run; assert_success; }

@test "\$SHTS_TESTS " {
  assert_dir_exist "${SHTS_TESTS}";
}

@test "\$SHTS_OUTPUT " {
  assert test -n "${SHTS_OUTPUT}";
}
